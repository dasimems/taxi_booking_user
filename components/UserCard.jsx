import { StyleSheet, Text, View, Image } from 'react-native'
import React from 'react'

const UserCard = ({active, data}) => {
  return (
    <View>
        
        <Image source={""} />
      <Text>UserCard</Text>
    </View>
  )
}

export default UserCard

const styles = StyleSheet.create({})